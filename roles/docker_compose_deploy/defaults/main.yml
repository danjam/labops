---
# Docker Compose Deploy role - default variables
# Controls Docker Compose deployment configuration

# Directory containing docker-compose.yml
docker_compose_project_dir: "."

# Docker compose file name
docker_compose_file: docker-compose.yml